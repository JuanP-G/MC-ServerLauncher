<ui:FluentWindow x:Class="McServerLauncher.Views.WhatsNewDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:loc="clr-namespace:McServerLauncher.Localization"
                 Title="{loc:Loc Whatsnew_Title}"
                 Width="560" SizeToContent="Height"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{loc:Loc Whatsnew_Title}" />

        <StackPanel Grid.Row="1" Margin="20,8,20,8">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <ui:SymbolIcon Symbol="Sparkle24" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0" />
                <TextBlock x:Name="HeaderText" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>

            <TextBlock x:Name="BodyText" TextWrapping="Wrap" FontSize="14" LineHeight="22" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,12,20,16">
            <ui:Button Appearance="Primary" Content="{loc:Loc Whatsnew_Ok}" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
